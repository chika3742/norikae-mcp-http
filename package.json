{
  "name": "norikae-mcp",
  "version": "0.2.0",
  "description": "Yahoo!乗換案内を使った日本の電車乗り換え検索MCP (Cloudflare Workers / Streamable HTTP)",
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "start": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "cf-typegen": "npx wrangler types",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "transit",
    "japan",
    "train",
    "乗り換え",
    "yahoo",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "agents": "^0.9.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
